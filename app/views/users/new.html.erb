<div id="clearance" class="sign-up">
  <h2><%= t(".title") %></h2>
  <%= simple_form_for @user do |f| %>
    <%= render 'form', f: f %>
    <div class="other-links" style="margin: 10px 875px">
      <%= link_to t(".sign_in"), sign_in_path %>
    </div>
  <% end %>
</div>
